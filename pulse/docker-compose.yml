services:
  pulse:
    build: .
    container_name: pulse-bot
    restart: unless-stopped
    ports:
      - "8080:3000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
